--- a/drivers/input/misc/Kconfig
+++ b/drivers/input/misc/Kconfig
@@ -892,4 +892,11 @@ config INPUT_STPMIC1_ONKEY
 	  To compile this driver as a module, choose M here: the
 	  module will be called stpmic1_onkey.
 
+config INPUT_LANDISK_R8C_KEYS
+	tristate "R8C Keys on I-O DATA LAN DISK series"
+	depends on MFD_LANDISK_R8C
+	help
+	  Say Y here if you want to enable key reporting from R8C MCU on
+	  I-O DATA LAN DISK series.
+
 endif
--- a/drivers/input/misc/Makefile
+++ b/drivers/input/misc/Makefile
@@ -46,6 +46,7 @@ obj-$(CONFIG_INPUT_IQS269A)		+= iqs269a.
 obj-$(CONFIG_INPUT_IQS626A)		+= iqs626a.o
 obj-$(CONFIG_INPUT_KEYSPAN_REMOTE)	+= keyspan_remote.o
 obj-$(CONFIG_INPUT_KXTJ9)		+= kxtj9.o
+obj-$(CONFIG_INPUT_LANDISK_R8C_KEYS)	+= landisk-r8c-keys.o
 obj-$(CONFIG_INPUT_M68K_BEEP)		+= m68kspkr.o
 obj-$(CONFIG_INPUT_MAX77650_ONKEY)	+= max77650-onkey.o
 obj-$(CONFIG_INPUT_MAX77693_HAPTIC)	+= max77693-haptic.o
